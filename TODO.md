# TODO — ponytail debt ledger

Items deferred because the current code works and the ceiling hasn't been hit.

## ~ Split connector.py (done — scanner extracted to _scanner.py, connector.py ~600 lines) ~

## ~ Fold discover_services.py (folded — cli.py --discover now delegates to run_diagnostics) ~

## ~ Add type checker to CI (added — mypy in CI, config in pyproject.toml) ~
